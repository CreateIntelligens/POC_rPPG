# =============================================================================
# .env.example - VitalLens 應用程式環境變數範本
# 複製此檔案為 .env 並填入實際的設定值
# =============================================================================

# -----------------------------------------------------------------------------
# VitalLens API 設定
# -----------------------------------------------------------------------------
# VitalLens API 金鑰，用於商業級精準檢測
# 從 https://vitallens.com 獲取 API 金鑰
VITALLENS_API_KEY=your_vitallens_api_key_here

# -----------------------------------------------------------------------------
# 應用程式設定
# -----------------------------------------------------------------------------
# 應用程式標題，顯示在網頁標題和頁面中
APP_TITLE=VitalLens 生命體徵檢測器

# 預設檢測方法 (POS/VITALLENS/CHROM/G)
DEFAULT_METHOD=POS (免費)

# -----------------------------------------------------------------------------
# 檔案上傳限制
# -----------------------------------------------------------------------------
# 最大檔案大小 (MB)
MAX_FILE_SIZE_MB=50

# 最大影片持續時間 (秒)
MAX_VIDEO_DURATION_SECONDS=45

# -----------------------------------------------------------------------------
# 網路設定
# -----------------------------------------------------------------------------
# 應用程式內部監聽端口
APP_PORT=8894

# 外部存取端口 (Nginx 反向代理端口)
EXTERNAL_PORT=8894

# 信任的主機列表 (生產環境建議設定為具體域名)
TRUSTED_HOSTS=*

# -----------------------------------------------------------------------------
# CORS 設定
# -----------------------------------------------------------------------------
# 允許的來源域名列表，用逗號分隔
CORS_ALLOW_ORIGINS=http://localhost:8894,https://localhost:8894,http://127.0.0.1:8894,https://127.0.0.1:8894

# -----------------------------------------------------------------------------
# 開發環境設定
# -----------------------------------------------------------------------------
# 設定為 true 啟用開發模式
TESTING=false
