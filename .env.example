# =============================================================================
# VitalLens 應用程式環境變數配置
# 複製此檔案為 .env 並填入您的實際配置值
# =============================================================================

# VitalLens API 設定 ----------------------------------------------------------------
# 從 https://platform.vitallens.com 獲取您的 API 金鑰
# 如果不設定，將無法使用 VITALLENS 商業檢測方法
VITALLENS_API_KEY=your_api_key_here

# 應用程式設定 ---------------------------------------------------------------------
# 預設的生命體徵檢測方法
# 可選值: "VITALLENS (需要 API Key)", "POS (免費)", "CHROM (免費)", "G (免費)"
DEFAULT_METHOD=POS (免費)

# 應用程式顯示標題
APP_TITLE=VitalLens 生命體徵檢測器

# 檔案上傳限制 ---------------------------------------------------------------------
# 單個影片檔案的最大大小（MB）
# 影響記憶體使用和處理時間，建議不超過 100MB
MAX_FILE_SIZE_MB=100

# 網路設定 -------------------------------------------------------------------------
# 應用程式監聽端口
# Docker 環境中會被容器端口映射覆蓋
APP_PORT=8894

# 外部訪問端口（Docker 主機端口）
# 用於 docker-compose 的端口映射
EXTERNAL_PORT=8894

# CORS 設定 ------------------------------------------------------------------------
# 允許的跨域來源，用逗號分隔
# 預設允許本地開發環境訪問
CORS_ALLOW_ORIGINS=http://localhost:8894,https://localhost:8894,http://127.0.0.1:8894,https://127.0.0.1:8894
